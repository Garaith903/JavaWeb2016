# Local server
server:
  # port is used by spring-boot-admin
  port: 8080
  contextPath: /arduinoob
spring:
  datasource:
    password: 
    url: jdbc:mysql://localhost:3306/arduinoobdb?user=root&autoReconnect=true&useSSL=false